{
  "name": "autoestetia-pro",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm run build",
    "start": "cd backend && npx prisma db push && node dist/index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
